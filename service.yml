apiVersion: v1
kind: Service
metadata:
  name: bms-service
  namespace: bms-sunil
spec:
  type: LoadBalancer
  selector:
    app: bms
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
